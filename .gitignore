# 依賴與建置產物
node_modules/
.next/
out/
*.log
*.tsbuildinfo
next-env.d.ts

# 個資:絕對不進版本庫(repo 將公開給 GitHub Pages 用)
# 舊履歷原始檔(含電話地址)
/chang-yu-cheng-resume.pdf
# 個資資料源(CI 以 Secret 注入)
resume.private.yaml
# 建置產出的 PDF(CI 重新產生)
public/*.pdf

# 素材原始檔:header 標誌原圖 4MB,只上 public/ 的 128px 縮圖
/johnny_icon.png
# 大頭照原圖備份(任何格式),只上 public/ 的 512px 縮圖
/avatar-original.*
# 簽名字標原始 SVG:已 inline 進 SignatureWordmark.tsx,原檔留本機當素材
/chang-yu-cheng-icon.svg

# 本機參考文件(個人導覽筆記,不進公開 repo)
/docs/CODEBASE-GUIDE.md

# 本機環境
.env*
.DS_Store
