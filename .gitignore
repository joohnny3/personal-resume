# 依賴與建置產物
node_modules/
.next/
out/
*.log
*.tsbuildinfo
next-env.d.ts

# 個資:絕對不進版本庫(repo 將公開給 GitHub Pages 用)
# 舊履歷原始檔(含電話地址)
/chang-yu-cheng-resume.pdf
# 個資資料源(CI 以 Secret 注入)
resume.private.yaml
# 建置產出的 PDF(CI 重新產生)
public/*.pdf

# 素材原始檔:header 標誌原圖 4MB,只上 public/ 的 128px 縮圖
/johnny_icon.png
# 大頭照原圖 2.3MB 備份,只上 public/ 的 512px 縮圖
/avatar-original.jpg

# 本機環境
.env*
.DS_Store
